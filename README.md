# neorv32-cfu-exercise
This repository consits of the implementation of a custom NEORV32 instruction. It consists of two distinct files, one C file, from which the instruction is called and one VHDL file, in which the instruction is implemented and inserted into the NEORV32 decoder unit
